name: elastic beat release by goreleaser

on:
  push:
    tags:
      - '*'

env:
  GOPRIVATE: github.com/streamnative

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      -
        name: Set up Go
        uses: actions/setup-go@v1
        with:
            go-version: 1.13.x

      -
        name: Debug
        run: |
            export GOPATH=/home/runner/work/go
            mkdir -p $GOPATH/src/github.com/streamnative/ &&
            ls -la

      -
        name: Checkout
        uses: actions/checkout@v2
        with:
            path: go/src/github.com/streamnative
    
      -
        name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v1
        with:
          workdir: /home/runner/work/go/src/github.com/streamnative/pulsar-beat-output/
          version: latest
          args: release --rm-dist --release-notes=docs/release-note.md --release-header=docs/release-header.md
        env:
          GITHUB_TOKEN: ${{ secrets.BEAT_ACCESS_TOKEN }}